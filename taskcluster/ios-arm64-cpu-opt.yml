build:
  template_file: darwin-opt-base.tyml
  routes:
    - "index.project.deepspeech.tensorflow.pip.${event.head.branch}.ios-arm64"
    - "index.project.deepspeech.tensorflow.pip.${event.head.branch}.${event.head.sha}.ios-arm64"
    - "index.project.deepspeech.tensorflow.pip.ios-arm64.${event.head.sha}"
  maxRunTime: 28800
  args:
    tcsetup: ""
    tcbuild: "--ios-arm64"
  metadata:
    name: "TensorFlow iOS ARM64 TFLite"
    description: "Building TensorFlow for iOS ARM64, TFLite, optimized version"
